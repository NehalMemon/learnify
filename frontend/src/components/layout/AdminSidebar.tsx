'use client';

import { Sidebar, type SidebarProps } from '@/components/admin/Sidebar';

export function AdminSidebar(props: SidebarProps) {
  return <Sidebar {...props} />;
}

export default AdminSidebar;

